In Python, the capitalize() method is a built-in string function that returns a copy of the original string with the first character capitalized and the rest of the characters converted to lowercase.
